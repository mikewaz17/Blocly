<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_share"
        android:icon="@drawable/ic_share_selector"
        android:orderInCategory="0"
        android:title="@string/menu_share"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search_selector"
        android:orderInCategory="1"
        android:title="@string/menu_search"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_refresh"
        android:orderInCategory="2"
        android:title="@string/menu_refresh"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_mark_as_read"
        android:orderInCategory="3"
        android:title="@string/menu_mark_as_read"
        app:showAsAction="never" />
</menu>
<!--#46 Putting the menu items in the ActionBar by assigning the orderInCategory attr a number "0"
The showsAction attr tells if and when a menu item will appear as an action button. The action_search
is set with always to make sure its always presented as a button. Other options included are ifRoom and never.
-->